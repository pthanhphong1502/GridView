import { ToggleAnimationDirective } from './toggle-animation.directive';

describe('ToggleAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
